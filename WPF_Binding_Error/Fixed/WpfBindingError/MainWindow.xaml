<Window x:Class="WpfBindingError.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfBindingError"
        mc:Ignorable="d"
        Title="MainWindow" Height="320" Width="240" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid x:Name="Grid" DataContext="{Binding ViewModel}">
        <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{Binding HelloText}"/>
    </Grid>
</Window>
